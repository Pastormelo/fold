import { NotBuiltYet } from '@/components/not-built-yet'

export const metadata = { title: 'Guests · Fold' }

export default function Page() {
  return (
    <NotBuiltYet
      title="Guests"
      willHold="Sunday arrivals, who is following up, and the coffee escalation when a question needs an elder rather than a volunteer."
      backedBy="Nothing built behind this one yet."
    />
  )
}
