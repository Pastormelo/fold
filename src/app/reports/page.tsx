import { NotBuiltYet } from '@/components/not-built-yet'

export const metadata = { title: 'Reports · Fold' }

export default function Page() {
  return (
    <NotBuiltYet
      title="Reports"
      willHold="Charts, the narrative builder, and elder meeting mode. The handoff deferred this deliberately as least useful before there is real data."
      backedBy="Deferred by §10 of the handoff rather than overlooked."
    />
  )
}
